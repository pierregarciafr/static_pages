<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate @users, class: 'digg_pagination' %>

<ul class="users">
      <%= render @users %>
</ul>

<%= will_paginate @users, class: 'digg_pagination' %>


